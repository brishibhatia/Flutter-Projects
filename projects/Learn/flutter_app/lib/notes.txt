Use Wrap widget instead of Row so that when the row elements start overflowing it will automatically shifted to next line.
Stateless widget is an abstract class and we just override its methods in the classes we extend
whenever we want to give decoration to listtile wrap in container and do all decoration in contaner itself.
SafeArea insets its child with padding to avoid operating system intrusions, using the greater of minimum or safe area padding.BUt to give full screen view we should avoid it.
Always write the name of the file in lowercase and the class name in camelcase
state -> over the override
Use valueNotifier and valuelistenablewidget for state varible accross the app(can be used in Stateless)
Either use nested valuelistenablewidget or ListenableBuilder with listenable.merge
Use ValueListenableBuilder → when you need the .value (like isDarkMode.value).AND Use ListenableBuilder → when you merge or listen to several notifiers (no direct .value).
A TextEditingController is a special controller class in Flutter used to:-> Read , Modify, or Listen to changes in the text inside a TextField or TextFormField.
arrow function is used if we have only one function to execute but if we have multiple (){},
If we use .adaptive with checkbox or CheckboxListtile then it will automatically adapt to the device UI
Use SingleChildScrollView which -> is a scrolling widget in Flutter that lets you make its child scrollable when the content overflows the screen.
If you want a nice ripple(splash) effect (Material look) → use InkWell. , If you need custom gesture control (like drag/swipe or non-Material look) → use GestureDetector
